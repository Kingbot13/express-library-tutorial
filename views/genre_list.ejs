<!DOCTYPE html>
<html lang="en">
<%- include('./head');  %> 
<body>
    <%- include('./layout') %>
    <h1><%= title %> </h1>
    <ul>
        <% genre_list.forEach(genre => {%>
            <li>
                <a href="<%= genre.url %> "><%= genre.name %> </a>
            </li>

        <%}); %>
    </ul> 
</body>
</html>